#include <stdio.h>
#include <stdlib.h>


void main (void) {
    int n;

    printf(Put number n);
    scanf(%i, &n);

    if(n >= 2) {
        for(int i = 2; i <= n; i++) {
            int itSimple = 1;
            for(int j = 2; j < i; j++) {  // j*j<=i
                if (i % j == 0) {
                    itSimple = 0;
                    break;
                }
            }
            if(itSimple) {
                printf(%d , i);
            }
        }

    } else {
        printf([ERROR] Number most more 2!
);
    }
}
